spring.application.name=SmartHomeEnergyAuth

# ===============================
# DATABASE CONFIGURATION (MySQL)
# ===============================
# Ensure the database 'smart_energy_db' exists in MySQL Workbench
spring.datasource.url=jdbc:mysql://localhost:3306/smart_home_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=swetha
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE CONFIGURATION
# ===============================
# 'update' automatically creates/updates tables without deleting data
spring.jpa.hibernate.ddl-auto=update
# Shows SQL queries in the console (useful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===============================
# EMAIL CONFIGURATION (Gmail)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dibyapatra954@gmail.com
# NOTE: Do NOT use your regular Gmail password.
# Go to Google Account -> Security -> 2-Step Verification -> App Passwords to generate one.
spring.mail.password=tyrm qeqm beuj zuuf

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# ===============================
# THYMELEAF CONFIGURATION
# ===============================
# Disable cache so you see HTML changes immediately without restarting
spring.thymeleaf.cache=false